# Firefox AMO Tags — Volume & Video Master 1000%

## Selected Tags

These are the most relevant tags from the available list:

1. **music** — Audio enhancement features (bass boost, voice boost, mono)
2. **streaming** — Works on streaming platforms (YouTube, Netflix, Twitch)
3. **youtube** — Most popular video platform users will use this on
4. **twitch** — Popular streaming platform where volume boost is essential
5. **mp3** — Audio file format the extension enhances

## Why These Tags?

| Tag | Relevance |
|-----|-----------|
| `music` | Core feature — volume boost, bass boost, mono audio |
| `streaming` | Primary use case — video streaming platforms |
| `youtube` | #1 platform users will search for volume boost extensions |
| `twitch` | #2 platform — stream audio is often too quiet |
| `mp3` | Audio format enhancement (browser playback) |

## Tags NOT Selected (and why)

| Tag | Why Not Selected |
|-----|------------------|
| `privacy` | Extension doesn't focus on privacy features |
| `security` | Not a security tool |
| `download` | Doesn't download anything |
| `video downloader` | Doesn't download videos |
| `video converter` | Doesn't convert formats |
| `vpn` | Not a VPN |
| `ad blocker` | Doesn't block ads |
| `content blocker` | Doesn't block content |

## Submission

Tags entered in AMO:
```
music, streaming, youtube, twitch, mp3
```

These 5 tags accurately describe the extension's core functionality without misleading users.
